<Window x:Class="ComicBin.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ComicBin.Desktop"
        xmlns:vm="clr-namespace:ComicBin.Desktop.ViewModels"
        xmlns:view="clr-namespace:ComicBin.Desktop.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type vm:HomePageViewModel}">
                <view:HomePage />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SettingsPageViewModel}">
                <view:SettingsPage />
            </DataTemplate>
        </Grid.Resources>
        <StackPanel Grid.Column="0" Background="Red">
            <Button Content="Home"
                     />
            <Button Content="Settings"
                   />
            <Button Content="About"
                     />
        </StackPanel>

        <ContentControl Grid.Column="1"
                        Content="{Binding}" />
    </Grid>
</Window>
